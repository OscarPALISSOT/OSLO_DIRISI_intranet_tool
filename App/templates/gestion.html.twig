{% extends 'sidenav.html.twig' %}

{% block titleSidenav %}

{% endblock %}

{% block stylesheetsSidenav %}
    {{ encore_entry_link_tags('sidenav') }}
    {% block styleSheetsGestion %}

    {% endblock %}
{% endblock %}

{% block javascriptsSidenav %}

{% endblock %}

{% block sideNav %}
    <li class="sideNavItem">
        <a href="{{ path ('Gestion', {role: role})}}" class="">Accueil {{ role }}</a>
    </li>

    {% if role != 'BCS' %}
        <li class="sideNavItem">
            <a href="{{ path ('Bnr', {role: role})}}" class="">BNR</a>
        </li>
        <li class="sideNavItem">
            <a href="{{ path ('contactCirisi') }}" class="">MODIP</a>
        </li>
    {% endif %}
    {% if role != 'BPT' %}
        <li class="sideNavItem">
            <a href="{{ path ('contactBdd') }}" class="">MIM3</a>
        </li>
        <li class="sideNavItem">
            <a href="{{ path ('contactBdd') }}" class="">OPERA</a>
        </li>
    {% endif %}

{% endblock %}


{% block body %}

{% endblock %}




