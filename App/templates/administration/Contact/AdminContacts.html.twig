{% extends 'base.html.twig' %}

{% block title %}OSLO | Administration des contact{% endblock %}

{% block stylesheets %}

{% endblock %}

{% block javascripts %}

{% endblock %}

{% block body %}

    <div id="main" class="mx-auto flex-column">
        <h1>Tableau de bord des contacts</h1>
        <div class="container">
            <div class="col mb-2">
                <a href="{{ path ('Admin') }}" class="btn btn-outline-secondary">Retour</a>
            </div>
            <div class="row">
                {# card Bases de défense #}
                <div class="col-lg-3 col-6">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title"><i class="fas fa-id-card"></i>Contacts bases de défense</h5>
                            <p class="card-text">Il y a actuellement {{ nbContactBdd}} contacts de bases de défense enregistrés</p>
                            <a href="{{ path ('Admin_Bdd') }}" class="btn btn-outline-primary">Voir plus</a>
                        </div>
                    </div>
                </div>
                {# /card Bases de défense #}
                {# card RFZ#}
                <div class="col-lg-3 col-6">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title"><i class="fas fa-id-card"></i>Contacts CIRISI</h5>
                            <p class="card-text">Il y a actuellement {{ nbContactCirisi }} contacts de CIRISI enregistrés</p>
                            <a href="{{ path ('Admin_Rfz') }}" class="btn btn-outline-primary">Voir plus</a>
                        </div>
                    </div>
                </div>
                {# /card RFZ #}
                {# card Contact#}
                <div class="col-lg-3 col-6">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title"><i class="fas fa-id-card"></i>Contact</h5>
                            <p class="card-text">Il y a actuellement  contacts enregistrés</p>
                            <a href="{{ path ('Admin') }}" class="btn btn-outline-primary">Voir plus</a>
                        </div>
                    </div>
                </div>
                {# /card Contact #}
            </div>
        </div>
    </div>
{% endblock %}